{
  "builds": [
    {
      "src": "api/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "api/wsgi.py" }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "api.settings"
  }
}
